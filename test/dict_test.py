import unittest
from dict import *

class json_search_test(unittest.TestCase):
    def test_id(self):
        self.assertTrue([]!=fetch(f))
    def test1(self):
        for i in list1:
            a = i
            b = fetch(f)[a]
            self.assertEqual(type(b), str)
